// values
const people = ["Gbemu", "Terra", "Yamah"];

// regular function
function sayHi(name) {
  console.log("Hi " + name);
}

sayHi("Moses");

// arrow function
const addNumbers = (a, b) => {
  return a + b;
};

addNumbers(212, 303);

// higher order functions
/**
 * timeout
 * timer interval
 * clear interval
 */
